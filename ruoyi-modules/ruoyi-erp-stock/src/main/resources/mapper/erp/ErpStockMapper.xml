<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.erp.stock.mapper.ErpStockMapper">

    <resultMap type="ErpStock" id="ErpStockResult">
        <result property="stockId"       column="stock_id"       />
        <result property="warehouseId"   column="warehouse_id"   />
        <result property="materialId"    column="material_id"    />
        <result property="quantity"      column="quantity"       />
        <result property="materialCode"  column="material_code"  />
        <result property="materialName"  column="material_name"  />
        <result property="warehouseName" column="warehouse_name" />
        <result property="safeStock"     column="safe_stock"     />
        <result property="unit"          column="unit"           />
        <result property="purchasePrice" column="purchase_price" />
        <result property="specification" column="specification"  />
        <result property="amount"        column="amount"         />
        <result property="createBy"      column="create_by"      />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"      column="update_by"      />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectErpStockVo">
        select s.stock_id, s.warehouse_id, s.material_id, s.quantity,
               m.material_code, m.material_name, m.safe_stock, m.unit, m.purchase_price,
               m.specification, round(s.quantity * m.purchase_price, 2) as amount,
               w.warehouse_name,
               s.create_by, s.create_time, s.update_by, s.update_time
        from erp_stock s
        left join erp_material m on m.material_id = s.material_id
        left join erp_warehouse w on w.warehouse_id = s.warehouse_id
    </sql>

    <select id="selectErpStockList" parameterType="ErpStock" resultMap="ErpStockResult">
        <include refid="selectErpStockVo"/>
        <where>
            <if test="warehouseId != null"> and s.warehouse_id = #{warehouseId}</if>
            <if test="materialId != null"> and s.material_id = #{materialId}</if>
            <if test="materialName != null and materialName != ''"> and m.material_name like concat('%', #{materialName}, '%')</if>
            <if test="warehouseName != null and warehouseName != ''"> and w.warehouse_name like concat('%', #{warehouseName}, '%')</if>
        </where>
        order by s.stock_id desc
    </select>

    <select id="selectErpStockById" parameterType="Long" resultMap="ErpStockResult">
        <include refid="selectErpStockVo"/>
        where s.stock_id = #{stockId}
    </select>

    <select id="selectErpStockByWarehouseAndMaterial" resultMap="ErpStockResult">
        <include refid="selectErpStockVo"/>
        where s.warehouse_id = #{warehouseId} and s.material_id = #{materialId}
    </select>

    <insert id="insertOrAddStock">
        insert into erp_stock(warehouse_id, material_id, quantity, create_by, create_time, update_by, update_time)
        values(#{warehouseId}, #{materialId}, #{quantity}, 'admin', sysdate(), 'admin', sysdate())
        on duplicate key update quantity = quantity + #{quantity}, update_by = 'admin', update_time = sysdate()
    </insert>

    <insert id="insertErpStock" parameterType="ErpStock" useGeneratedKeys="true" keyProperty="stockId">
        insert into erp_stock
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="warehouseId != null">warehouse_id,</if>
            <if test="materialId != null">material_id,</if>
            <if test="quantity != null">quantity,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="warehouseId != null">#{warehouseId},</if>
            <if test="materialId != null">#{materialId},</if>
            <if test="quantity != null">#{quantity},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>

    <update id="updateErpStock" parameterType="ErpStock">
        update erp_stock
        <trim prefix="SET" suffixOverrides=",">
            <if test="warehouseId != null">warehouse_id = #{warehouseId},</if>
            <if test="materialId != null">material_id = #{materialId},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where stock_id = #{stockId}
    </update>

    <delete id="deleteErpStockById" parameterType="Long">
        delete from erp_stock where stock_id = #{stockId}
    </delete>

</mapper>
